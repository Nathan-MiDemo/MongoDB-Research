import random

class IndexGenerator:
	def __init__(self):
		#mime_types is a dict of all the possible mime types. Each type is
		#associated with a file extension (.gif, etc)
		self.mime_types = 
		{
			"image":
			{
				"gif": ".gif",
				"jpeg": ".jpg",
				"png": ".ping",
				"tiff": ".tiff"
			}
			"audio":
			{
				""
			}
		}
